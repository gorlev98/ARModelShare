// Re-export types from shared schema
export type {
  Model,
  InsertModel,
  SharedLink,
  InsertSharedLink,
  QROptions,
  AnalyticsStats,
  ActivityEvent,
  ValidationStage,
  UserProfile,
} from '@shared/schema';
